<div>
            <h3>Bathroom</h3>
            <Checkbox
                checked={this.state.bathroom}
                onChange={this.handleChange('bathroom')}
                value="bathroom"
                color="primary"
            />
            <div className={classes.batDrop} id="bathroomDrop">
                <Bathroom update={this.updateTotal} currentValue={this.checkTotal}/>
            </div>
        </div>
        <div>
            <h3>Addition</h3>
            <Checkbox
                checked={this.state.addition}
                onChange={this.handleChange('addition')}
                value="addition"
                color="primary"
            />
            <div className={classes.batDrop} id="additionDrop">
                <Addition update={this.updateTotal} currentValue={this.checkTotal}/>
            </div>
            </div>